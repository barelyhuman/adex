export {
  hydrate,
  Router,
  Route,
  lazy,
  LocationProvider,
  ErrorBoundary,
  useLocation,
  useRoute,
  prerender,
} from 'preact-iso'

export declare const join: (...args: string[]) => string
